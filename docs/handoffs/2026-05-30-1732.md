# Next-session handoff

_Written: 2026-05-30 17:32 PT_

## Branch & state
- Branch: `main` (worktree: N/A)
- Uncommitted: 1 file — `.gitignore` (added `.claude/next_prompt.md` ignore line via /handoff)
- Behind/ahead of origin: up to date

## What shipped this session
- 15948a5 — docs: ADR 013 core/overlay model (accepted, amended) (#8) — MERGED, branch deleted

## What's blocked & why
- Nothing blocked.

## Exact next steps
1. Commit the `.gitignore` change (or leave it — session-private handoff ignore).
2. When **org #2** appears, build the install-time override mechanism. Spec lives in `docs/ADRs/013-core-overlay-model.md` → "Amendments" section. Three must-fixes:
   - Precedence: `merge_json` is target/user-wins — opposite of `core < overlay`. Need explicit ordering (`core < overlay`, then `overlay < project/user`), not a naive `config-merger` reuse.
   - CLAUDE.md: `append_stack_section` supports one marker pair — add distinct `<!-- ORG_OVERLAY_MANAGED -->` marker for repo-init injection (`scripts/lib/tier-installer.sh:40-43`).
   - Ship the deltas-only lint ("no core file copied verbatim") *with* the mechanism — it's the only contract enforcement.
3. Override resolution semantic (decided, in ADR): deep-merge JSON / marker-replace CLAUDE.md / whole-file-replace documents. Mirror the per-type dispatch at `tier-installer.sh:37-47`.

## Gotchas
- CarboNet's private overlay (`CarboNet-Nano/carbonet-standards`) is being authored separately against the ADR 013 contract — do NOT build it from this repo. Core-side contract only.

## Cross-repo references
- `CarboNet-Nano/carbonet-standards` — org #1 overlay, built in an adjacent thread. Consumes the ADR 013 deltas-only contract.

## Team this session
- Used: none (direct review + edit + merge; no subagents dispatched).
